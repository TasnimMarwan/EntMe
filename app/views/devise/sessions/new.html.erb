
<main id="page-content" class="wrap container">
  <div id="banner" class="custom-padding jumbotron row"></div>
  <section class="page-section row col-split-2">
    <div class="col-xs-12 col-sm-6 col-md-4 col-md-offset-1">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {:id => "signin_form"}) do |f| %>
        <h2>Sign In</h2>
        <div class="row">
          
            <div class="col-xs-12 form-group">
              <%= f.text_field :email, autofocus: true, class: "form-control", placeholder: "Email", name: "email"%>
              <%= f.label :email %><br />
              <div class="invalid-feedback">
                Please enter a valid EMAIL
              </div>
            </div>
            <div class="col-xs-12 form-group">
              <%= f.text_field :password, autocomplete: "off", class: "form-control", placeholder: "Password", name: "password" %>
              <%= f.label :password %><br />
              <div class="invalid-feedback">
                Please enter a valid PASSWORD
              </div>
            </div>

            <div class="col-xs-12 form-group clearfix">
              <a class="btn btn-default forget-link pull-left" href="javascript:;">forgot your password?</a>
              <button class="btn btn-primary pull-right" type="submit" >Log In <i class="fa fa-lock"></i></button>
              <input type="hidden" name="redirect" value="">
            </div>
          
        </div>
      <% end %>
    </div>
  </section>
</main>